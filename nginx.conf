server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  # Las rutas de Angular se resuelven en el navegador, por eso
  # cualquier ruta desconocida vuelve a index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
